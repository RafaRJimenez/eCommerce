import React from 'react';

const Header = () => {
    return (
        <header className="App">
        <h1>logo</h1>
        <button>Login</button>
        </header>
    );
}

export default Header;
